

#include <iostream>
#include "Board.h"

struct list {
    int x;
    list* next;
};

int main() {
    std::cout << sizeof(8);
//    Board board {Board(Vec(10,5))};
//    board.add(Vec(2,4));
//    int x2 = board.add(Vec(3,1));
//    Vec v2 = board.find(x2);
//    std::cout << board.toString() ;
}

